# K2 centered physical-split scan — preregistration (2026-07-26)

This diagnostic tests whether the broad centered quotient enclosure is caused
by the physical split alone. It is fixed to cell 144,
`t=[72/25,29/10]`, `delta=[9/1000,1/100]`, midpoint `19/2000`, half-width
`1/2000`, spatial grid `192`, and Arb precision 140 bits. The only scanned
parameter is `physical_inner` in `(1181/1000,119/100,1199/1000)`.

For each split the script forms the same centered direct quotient and reports
the raw residual margin. This is not a certificate: companion tails and
truncation tails stay explicitly unresolved. No split may be selected
post-hoc for a theorem claim; a favorable scan only justifies a fresh dated
preregistration for a complete cover.
