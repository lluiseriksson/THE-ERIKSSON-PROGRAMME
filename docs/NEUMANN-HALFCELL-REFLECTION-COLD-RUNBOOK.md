# Half-cell block reflection: finite promoted geometry gate

Source59160603dda023d9f577ea7edc96f24995de1ac5; runner/verifierab13c0e75;
launcher9d6b2f2e8. Promotion reverses exactly to HOT sourcee46c93e3a after
public/private name mapping and removing only provenance/audit placement.
Two-file text guard0.1031689s/13856768bytes; import guard0.0824861s/12316672bytes.
Independent verifier synthetic tests passed0.1107253s/19275776bytes: these
are instrument checks, not compiler evidence. All local work is bounded I/O.

Open the immutable notebook only after publication. CPU/high RAM >=40GiB,
no GPU, account lluiseriksson@gmail.com. Exactly one cell execution. Expected
SOURCE_SHA59160603d and RUNNER_REVneumann-halfcell-block-reflection-cold-v1.
Stop on first actual child error. No project build output restoration;
official pinned Mathlib cache is allowed. Focal: NeumannHalfCellBlockReflection;
audit: its three public declarations. No root-wide oracle or exploratory CI.

Preserve outer archive neumann-halfcell-block-reflection-cold-v1-preservation-20260905.tar.gz,
verify its hash locally plus inner records/output/axioms with the pinned
independent verifier. Only then retire the two promoted headers. Preserve
all failed evidence. Delete runtime and close the sole tab after download.
The gate is an intermediate geometry seal, not Q*Q reflection or regional
inverse.20/41,TermSource0,window15 unattained remain unchanged.
