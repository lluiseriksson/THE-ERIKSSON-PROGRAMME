# M3 BRIDGE AUDIT — 2026-07-13 (phase 0 of the ">7 that shortens YM" search)

Owner question: can a next paper score >7 (target 8, absolute scale)
AND genuinely shorten the path to unconditional Yang-Mills?  Method:
the CLAUDE.md Part I instruction (c) executed literally — three
independent desks (hypothesis census, weakest-link prosecutor,
score-realism with web calibration) over the committed chain at
checkpoint ~9120a23b.  READ-ONLY audit; no code changed.  This
document is the registered synthesis; the full actas are in the
session record of 2026-07-13.

## Verdict in one line

THE CHAIN DIES AT THE BRIDGE, EXACTLY WHERE THE HOUSE INSTRUCTION
PREDICTED — not in the lemmas: the identification of
`covIR + covUV_concrete` with the truncated two-point function of
the ACTUAL Wilson Gibbs measure is not merely unproved, it is
UNFORMULATED in the formalization, and the currently proved IR input
(KP clustering) holds only at STRONG coupling while the hRpoly UV
programme lives at WEAK coupling — there is no coupling value at
which the proved and planned inputs are simultaneously about the
actual lattice YM measure.

## Census (desk 1, condensed)

- The M3 lane carries exactly ONE named analytic hypothesis:
  `hRpoly` as `SingleScaleUVDecay` (`YangMills/RG/
  SingleScaleUVDecay.lean:33`), consumed by
  `lattice_mass_gap_of_cluster_and_marginal_coupling`
  (`YangMills/RG/MarginalUVMassGap.lean:111`).
- BUT the assembly quantifies over free abstract `covIR, Rsc, g,
  nsc`; the identification with the physical correlator lives in
  the 30-field `CMP116RawSourceM3Frontier` record
  (`YangMills/RG/PhysicalGaugeCMP116RawM3.lean:241`) as explicit
  unproved fields (notably `rooted_hsharp_remainder_identity`).
- hRpoly closure would make the lattice M3 STATEMENT unconditional;
  M4/M5/Clay untouched (repo's own ~0% rule stands).
- Remaining registered obligations: B3, B4, volume-uniform
  Poincare, P4-ADJ (true adjoint model), Wilson-Hessian
  identification, covariance-root localization, G5 ("the wall"),
  O1 = hRpoly proper, P5 glue.

## Prosecution (desk 2, joint verdicts)

1. CT endpoint vacuity: HOLDS internally (no substitute operator,
   non-vacuity audited) — but the object is FOUR identifications
   from the section-6.3 operator (free shell, trivial background,
   trivial adjoint model, fine-bond metric vs modified metric).
2. Fixed volume: DIES as a route — `CP` from compactness, no
   registered volume-uniformity brick anywhere; aggravated by the
   proved lower bound `CP >= L^{d-2}` (coercivity collapses with
   block size in d=4).
3. "Sole carried hypothesis": DIES as a literal claim — the
   marginal coupling recursion, the regime-coherent IR bound, the
   correlator decomposition, and the open Appendix-F glue
   (O2/O4=B3/O5=B4 + the B2 method deviation) are additional
   conditionality a referee will count.  The fine print is honest;
   the headline sentence is not.
4. Shape of hRpoly: scalar interface type-checks; the CONSTANTS do
   not connect (Appendix-F demands kappa ~ 267 in d=4; the CT rate
   theta is orders of magnitude below and shrinks with L and
   volume; different metrics; no rescaling brick).  O2 is the
   repo's own registration of this risk.
5. Paper-world reality: hRpoly as consumed is NEW MATHEMATICS —
   Balaban proved UV STABILITY, never correlation decay or a gap;
   Dimock's machinery is phi^4_3 (relevant coupling) not YM
   (marginal); if the estimate were in the literature, the
   weak-coupling lattice gap would not be a famous open problem.

Weakest link: THE CORRELATOR BRIDGE (+ weak-coupling IR input) —
estimated 50-60% of the remaining distance to "M3 unconditional at
weak coupling"; hRpoly as scoped 35-45%; the freshly closed CT
endpoint ~2-3% (consistent with the external 3.1/10 calibration,
ledger Addendum 269).

Hygiene defect found in passing: ledger Addendum 269 committed with
unfilled placeholders `[JOBS]`/`[SPLIT269]`
(docs/VERIFICATION-LEDGER.md ~24123-24126) — flagged to the Part II
desk.

## Score realism (desk 3, calibrated against C5 = 6.00 absolute)

| Milestone | Honest band | Referee-kill risk |
|---|---|---|
| CT brick as standalone paper | 4.8-5.5 | 20-30% |
| hRpoly closed at FIXED volume | 6.0-6.6 | 35-50% |
| hRpoly volume-uniform + M3 unconditional WITH the correlator identification proved against the concrete Wilson measure | 7.3-8.0 if it survives; risk-weighted 6.3-7.2 | 50-70% |
| Same, paper-only (no Lean) | 5.5-6.5 | 70-85% |

Outside facts fixing novelty: weak-coupling 4d lattice gap OPEN
(Chatterjee's survey lists it as central; his 2024 SU(2) result is
via Higgs, not pure YM); Balaban-for-YM never assembled in accepted
form (Dimock reconstructed phi^4_3); Douglas et al. arXiv:2603.15770
(2026-03) formalized the FREE field in Lean — "first machine-checked
interacting-RG estimate" still claimable, "first machine-checked
constructive QFT" no longer.

## Consequence (instruction (d))

The two verdicts COINCIDE: the score desk's non-negotiable condition
for >7 (endpoint theorem over the concrete Wilson measure, regimes
coherent) is exactly the prosecutor's missing object.  Therefore the
theorem the surviving-audit logic demands is THE BRIDGE ITSELF, not
more activity bricks:

  (B-1) FORMULATE in Lean the identification statement — a
        multiscale decomposition of the actual Wilson-measure
        truncated two-point function into covIR + covUV, with the
        regime hypotheses explicit;
  (B-2) settle the REGIME COHERENCE question honestly: exhibit (or
        refute the existence of) a coupling window where an IR
        clustering input and the Balaban UV flow can be
        simultaneously instantiated for the same measure;
  (B-3) only then resume the activity ladder, whose value is
        conditional on (B-1)/(B-2).

(B-1) is cheap and strictly clarifying (it converts instantiation
freedom into named hypotheses — the same move that made hRpoly
honest).  (B-2) is the genuine research risk and the gate: if it
dies, the 7.3-8.0 milestone is unreachable by this chain and the
honest report says so.  No C6 charter is registered until the owner
picks; per the house rule, any charter will carry kill criteria and
a pre-registered score ladder judged externally before fabrication.
