# K4 t-box [2.965,2.970] boundary failure — 2026-07-26

The frozen endpoint contract rejects the continuous t-box
`[2.965,2.970]` on delta `[0.048,0.05]`.  Production and replay are
byte-identical; the only aggregate row above budget is

```text
nuD_main = [1.02348431232703 +/- 4.21e-15]
```

The failed pair is retained in
`scripts/surface_remainder_k4_tbox_2965_2970.txt` and its replay.  No positive
manifest is generated.  The next admissible diagnostic is a width-`0.0025`
split; no interpolation is inferred.
