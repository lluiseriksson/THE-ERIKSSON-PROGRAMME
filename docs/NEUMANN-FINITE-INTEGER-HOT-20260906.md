# Finite representative to physical counting mass — HOT diagnostic

Current source eb2fde64ff5597e6c3fa49d3beb114633e806d37; instrument
a3bbb0bbda604929bd57586e7387100c15021301. This is NOT a cold seal.
The previous v1 failure is preserved in ledger1135, not deleted or repeated.
Only the Mathlib-only repro import changed to Lean.Elab.Tactic.NormCast.
Physical draft bytes remain e4dee3b659ea6ceee7a114764a4f079f3c156a0ad83f6934c94362dbba320a74.

## Live checkpoint

One execution started at2026-09-06T08:20:28.965741UTC after runner hash gate,
PID20470, retained hosta7c701abce35. CPU/highRAM50.99GiB/noGPU, visible account
lluiseriksson@gmail.com. Existing checkout87edca651 remains clean, pinned
Mathlib07642720480157414db592fa85b626dafb71355b and Lean4.29.0-rc6.
This uses the retained cache; no cold claim and no new CI.

Mathlib repro passed in2.221527487s with the exact three-name axiom gate.
At the first read, physical_prerequisites was active (8180/8378 jobs shown).
The actual physical draft has NOT emitted a verdict yet. Successful prefixes
must not be reported as completion. Do not relaunch the original cell or runner.

- Console: /content/finite-integer-hot-v2-console.log
- Records: /content/neumann-finite-integer-kernel-hot-v2-evidence/records.json
- Current build log: /content/neumann-finite-integer-kernel-hot-v2-evidence/physical_prerequisites.log
- Final archive: /content/neumann-finite-integer-kernel-hot-v2-evidence.tar.gz
- Final exit: /content/finite-integer-hot-v2-exit.txt
- Distinct source paths: tmp/neumann-finite-integer-kernel-hot-v2/*.lean
- Runner SHA256:6334e8d3c6946b8313f24cd47b793e03bad6c8770fc4b4193ac73e7432b10f73

Browser tab4 retains the session. Original notebook Cell1 is completed cold
evidence, NEVER rerun. Cell2 is only a log reader/transfer helper; it does not
launch Lean. Terminal is occupied by the one runner above. Check near the
known physical build threshold, not by minute-by-minute polling.

## Acceptance and continuation

After prerequisites: compile the four-name physical draft, strict axiom gate,
clean tracked source check, archive actual outputs/logs/pins and real exits.
On first error preserve its full log. Never infer PASS from successful imports.
Download and independently verify using
scripts/verify_neumann_finite_integer_kernel_hot_v2.py. Its instrument-only
mutation test accepted1 synthetic fixture and rejected16; exit0/0.3173452s,
24051712 observed peak RSS bytes, under30s/512MiB watchdog. Synthetic output
bytes are explicitly fake and are NOT mathematical evidence.

Only an actual verified HOT PASS permits exact promotion; the later cold
checkout must not restore this project's outputs. Keep PRE-VALIDATION until
that later evidence exists. Retain runtime only for a prepared bounded repair
or follow-up, otherwise preserve evidence and disconnect/delete it.

## Next design gate, not extra source fabricated on an unchecked draft

The already sealed retained counting-mass equality can expose the SAME
integer indicator after the explicit kernel passes. It does not supply a
Neumann Laplacian or regional inverse. For the actual rectangle, use the
constructed site equivalence and generated lifted side B*m, not a free map.
The common (k,branch) image equality of two owner indicators is not the
one-sided image-sum/operator interchange. That separate finite/infinite
summation bridge must identify which variable is summed and retain the
counting coefficient (M^-d)^(2*depth); no weighted-adjoint substitution.

20/41; TermSource0; window15 compatible/not attained. Goal active.

## Continuation08:34UTC: live process and independent next repro

PID20470 was checked directly through psutil: command
`python3 -u /content/launch-finite-integer-hot-v2.py`, live at842seconds,
status sleeping while waiting for its child. Exit file still absent; build log
advanced into BalabanCMP99BlockContour. This is a verified wait, not runtime
loss or failure. No new scientific process was launched.

An independent step5 draft is committed atf9e6f5eb044e22424a5ed0732bc581b94d8c8b3c:
tmp/NeumannImageIntervalCoverageDraft.lean. It imports ONLY sealed orbit
algebra plus Mathlib, not the unverified finite-Q draft. Its seven proposed
declarations decode quotient/remainder for both branches and target bijectivity
of `(k,branch,n in [0,m)) -> printed integer image`. This is NOT compiled,
NOT d-dimensional coverage yet, and NOT an image-sum interchange or inverse.

Text/import/delimiter guards passed exit0/0.1785039s/10162176observed peak
RSS bytes. Exact Git-blob extraction of a Mathlib-only repro passed
exit0/0.1473817s/16416768observed peak bytes, no local Lean. Draft SHA256
baa0e468d676632ec4e89feec5028c0202acc6acff309ad59b07a4040064f085;
printed orbit source25a68943ae80ca3dcb4dbd22f2db7e10501d7b10c021692c84870f8a1fbb843a;
verbatim reproccfc6aa7e6a850256de48d93113a8b237079d5bad43bfd8cfcbad48b5e948c8d.
Generator scripts/neumann_image_interval_repro_contract.py preserves all
seven proof statements/bodies and copies only the exact three orbit definitions.

Do not run that next diagnostic concurrently. If current finite-Q gate fails,
preserve and repair ONLY its first error. If it passes, independently preserve
it before any prepared HOT follow-up or exact promotion. No cold seal follows
from either textual gate. Keep the retained runtime for that bounded sequence.
