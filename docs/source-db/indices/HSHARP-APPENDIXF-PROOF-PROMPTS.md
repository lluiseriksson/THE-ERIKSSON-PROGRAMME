# H# Appendix-F Proof Prompts

## Prompt A - Activity Bound Feed

Open `proof.dimock.appendixf.hsharp-feed`,
`proof.activity.termwise.live-fields.v2`,
`proof.activity.termwise-identification`,
`cmp116.lemma3.window.2.14-2.38`, and
`cmp116.lemma3.final-2.38`.

Return:

- exact source statement for `|H(X)| <= H0 exp(-kappa d_M)`;
- hypotheses for `H0 <= c0` and `kappa >= 3*kappa0+3`;
- source metric and modified metric convention;
- proposed Lean premise feeding `balabanCMP116AppendixFHsharpOfIntegratedKsharp`;
- blockers that remain before the Appendix-F H# route can consume the activity estimate.

Do not use the Appendix-F H# output as evidence for the upstream activity bound.

## Prompt B - Holes/Omega Dictionary

Open `crosswalk.dimock.appendixf-hole-cluster-route`, `dimockii.appendix-f.cluster-with-holes`, and repository Omega/skeleton declarations.

Return:

- exact source holes relation and connectivity convention;
- repository Omega/skeleton object that should consume it;
- whether the target is `omegaHolePolymerSystem_KPCriterion_volumeUniform_skeleton_exp_of_metric_bound` or an intermediate dictionary theorem;
- remaining skeleton metric dictionary blockers.

Do not replace the holes relation with ordinary polymer adjacency.

## Prompt C - Rooted H# Identity

Open `proof.rooted-hsharp-remainder.identity.v2` and `HSHARP-ROOTED-REMAINDER-HANDOFF.md`.

Return:

- exact physical Yang-Mills rooted H# representation target;
- relationship between the raw-source scale family and scalar `Rsc`;
- summability and real-part normalization requirements;
- Lean target for `BalabanCMP116SourceAssumptions.rooted_hsharp_remainder_identity`;
- upstream fields that must already be discharged.

Do not use the rooted H# identity to backfill Gaussian/root/Hessian/activity/support fields.

## Source-key and order invariant

For the H#/Appendix-F lane, keep `proof.dimock.appendixf.hsharp-feed` as the
route card and `proof.rooted-hsharp-remainder.identity.v2` as the downstream
rooted-identity card. The `proof.*` H# keys are repository live-field cards, not
primary sources; `dimockii.appendix-f.cluster-with-holes` and
`dimockii.appendix-f.second-ursell.645-646` are the extracted/partial Dimock
Appendix-F architecture; and `cmp116.lemma3.final-2.38` is only a
visual-confirmed activity-estimate endpoint until the CMP116/Balaban activity
dictionary is supplied.

Do not populate `balabanCMP116AppendixFHsharpOfIntegratedKsharp`,
`appendixFHoleExpWeight`,
`omegaHolePolymerSystem_KPCriterion_volumeUniform_skeleton_exp_of_metric_bound`,
or `rooted_hsharp_remainder_identity` from Dimock architecture, rooted H# output,
or the final Lemma 3 display alone. The open feed blockers remain the
`|H(X)| <= H0 exp(-kappa d_M)` activity/locality estimate, `H0 <= c0`,
`kappa >= 3*kappa0+3`, source metric and modified-metric dictionary,
Omega-connectivity, skeleton metric dictionary, raw-source scale-family link,
summability, and real-part normalization.
