# Physical value-action promotion — prepared, not executed

Prerequisite: durable preservation and independent verification of the
physical-prefix cold archive and the separate value-action HOT archive.
The current transport failure must not be relabelled mathematical FAIL,
nor silently waived for selective sealing.

## Exact source and scope

Draft source: 7bdc0fa1e76b3e2d3d64d1822bb30459d6697979.
Path: tmp/SourceFlowPhysicalValueActionDraft.lean.
Binary Git blob SHA256:
bfa16d9e39c9ee6518c9079cb61544d86864c6c87ad480af2e4ca5783bf76555.
This exact draft passed HOT, not a fresh promoted-source gate.

The future production file is BalabanCMP99SourceFlowPhysicalValueAction.lean,
with a separate BalabanCMP99SourceFlowPhysicalValueActionAudit.lean.
The transformation is restricted to provenance comments, moving the three
axiom prints to that audit, and the following public-name mapping:

- cmp99SourceFlowPhysicalRealGreen_typedKernel_draft -> cmp99SourceFlowPhysicalRealGreen_typedKernel
- cmp99SourceFlowPhysicalRealGreen_ownerAction_of_budget_draft -> cmp99SourceFlowPhysicalRealGreen_ownerAction_of_budget
- exists_cmp99SourceFlowPhysicalRealGreen_uniformValueAction_draft -> exists_cmp99SourceFlowPhysicalRealGreen_uniformValueAction

No hypothesis, constant, proof, import, private helper or mathematical
normalization is to change during promotion. Reverse the finite mapping
and compare mathematical text against the Git object before publication.
Keep PRE-VALIDATION in the new objects until their own cold evidence passes.

## Acceptance gates

1. The typed point kernel is for the literal real ambient source-flow Green.
   Whole-vector point transport and individual Lie-fibre norm identities
   must remain cited; no outer-space norm isometry may be introduced.
2. Orientation remains output owner / source owner. The only metric
   orientation change is finBoxDist_comm, not an adjoint action estimate.
3. Exactly one owner fibre contributes R^4. F4 gives C/R^2 and the result
   is exactly C*R^2, with R=L^(depth+1)>0. No total-owner count enters.
4. The final existential theorem internally supplies the intermediate
   amplitude budget. rho,C precede depth,K,Q,Nc; no free Green family,
   reconstruction equality, or per-depth witness replaces this endpoint.
5. A fresh exact-source Colab checkout runs necessary prerequisites,
   promoted focal, then the audit with exactly the three public names.
   One checkout and build directory serve this intermediate gate.
   No project-output cache restoration, exploratory CI or Windows Lean.
6. Preserve raw exits, immutable logs, source/output hashes, exact axiom
   sets and independently verified downloaded evidence before sealing.

This plan creates no production module and starts no computation.
Even a successful future seal closes only the full ambient value action.
Proper regional inverse/reflection and derivative estimates remain separate
named obligations. 20/41, TermSource=0, window15 unattained.
