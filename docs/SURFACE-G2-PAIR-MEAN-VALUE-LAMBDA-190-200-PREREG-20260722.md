# Preregistration: adjacent lambda cell for direct W-sign route

Before reading its result, register one cell with the existing beta cell

```text
beta   [13059/128,13060/128]
lambda [19/10,2]
modes 115; beta/lambda orders 50/50; Arb 500 bits
```

The unchanged mean-value driver must emit a production/replay pair.  A strict
negative upper endpoint is candidate evidence for the direct `W^J` route only;
it does not promote G2/G6.  A timeout or nonnegative enclosure is retained as
a route diagnostic.
