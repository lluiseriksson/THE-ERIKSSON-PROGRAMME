# Pair mean-value lambda cell `[1.9,2]` (2026-07-22)

The preregistered adjacent lambda cell at
`beta=[13059/128,13060/128]` passed with the unchanged 115-mode,
50/50-order, 500-bit configuration.  Production and replay are byte-identical;
the strict upper endpoint is approximately `-8.92947641338894e-109`.

It is archived as a single `CELL_CERTIFIED_CANDIDATE` with promotion `NONE`.
The wider next cell `[2,3]` was rejected separately, so this result does not
provide a bulk cover or G2/G6 load.
