# Incident: K4 t-box `[3.06,3.07]` design run timeout

The pre-registered candidate run used the frozen configuration
`delta=[1/25,81/2000]`, `t=[153/50,307/100]`, `seed_grid=12`,
`max_cells=2304`, and Arb precision 140.  The production driver was allowed
60 seconds and timed out before writing a transcript.  No result, fraction,
manifest, or gate promotion exists for this box.

This is an execution-budget failure only.  It is not evidence for or against
K4.  The preceding six-box chain remains candidate-only and unchanged.

The same pre-registered run was subsequently allowed 180 seconds and
completed in 58 seconds.  Production and replay are byte-identical and pass
the seven-row validator; the resulting seventh box is recorded as candidate
only in the 20260723 manifest.  The initial timeout remains retained as
provenance for the execution-budget observation.
