# K4 / S1''' / S2''' lane-status audit (2026-07-28)

## External bounded audit

Claude Fable 5 High was asked for a read-only dependency audit through the
local Fable Bridge using the explicitly selected profile `masterythief`
(`masterythief@gmail.com`).  The bridge reported
`verified_model: claude-fable-5` and no error.  The audit was instructed to
treat the running weak-main certificates as unverified.

Its conclusion was that K4 and the S1'''/S2''' weighted-remainder judges are
load-bearing only for the superseded sharp target `X_main>0`; the proposed
weak-main G2 route does not import them.  It also identified manuscript and
gate-board text that still describes the old sharp route.

## Independent local verification

The conclusion was checked against the repository rather than accepted on
model authority:

- `docs/SURFACE-G2-WEAK-TERMINAL-AUDIT-PREREG-20260728.md` enumerates the nine
  terminal inputs and explicitly prohibits the superseded sharp-positive K2
  transcripts and old coefficient route.
- `scripts/audit_surface_g2_weak_terminal_cover.py` imports the finite-role,
  G5, Q, near/far weak-main, relay, mirror, residual, and exact-algebra modules.
  It contains no K4 or S1'''/S2''' dependency.
- `docs/SURFACE-CLOSURE-GATES.md` already records G1 as
  `REMOVED_FROM_TERMINAL_PAPER` and says K4 remains a research lane rather than
  a theorem or submission gate.
- The K4 and weighted-judge entries in the closure notes belong to the
  delta-Taylor route for the stronger sharp premise.

Therefore, for the Surface Theorem through the weak-main route:

```text
K4 / S1''' / S2''' = superseded research lane, zero terminal load.
```

They are not claimed solved.  They would become load-bearing again if the
stronger sharp-positive result were restored as a theorem in its own right.

No G2 or manuscript promotion follows from this audit.  The weak-main
production/replay pairs and their independent validators must pass first.
