# S2 t-jet local probe — result

The preregistered point `(beta,t)=(20,2.9)` was run twice with
`pilot_cells=256`, `max_cells=2048`, and `seed_grid=4`.  Both transcripts
validate, and the selected numerical fields are byte-stable:

```text
effective_cells=2047
half_abs_upper=0.270511063588947822006056523802...
theta3=2.8640686974502770101547029683...
```

Thus the local diagnostic inequality `0.5*|Y''(t)| <= Theta3(c)` is enclosed
with positive margin at this point.  The `Y_t2` interval itself is still
wide (`[+/- 0.542]`), and there is no parameter-box cover, delta-zero patch,
or weighted global sum.  This result is therefore **candidate evidence only**
and does not alter S2, K2, K4, G2, G6, or the manuscript slot ledger.
