# Preregistration — 300-bit rescue box `[1705/16,853/8]`

Registered before execution: 2026-07-26  
Scope: one finite-beta candidate unit; no automatic G2/G6 promotion.

Frozen contract:

```text
beta_lo = 1705/16
beta_hi = 853/8
CWIN = 3/2
beta Taylor order = 40
t Taylor order = 50
Arb precision = 300 bits
minimum t width = 1/100000
```

The production command is:

```text
python scripts/run_surface_scaled_bulk_cwin3p2_rescue300.py \
  --unit gap106p5625_106p625_r300prereg \
  --lo 1705/16 --hi 853/8
```

Acceptance requires a contiguous moving-endpoint `t` cover with every
outward-rounded upper endpoint strictly negative, a complete transcript with
dependency hashes, a byte-identical replay, and an independent pass from
`validate_surface_scaled_bulk_cwin3p2_rescue300.py`.  A timeout or failure is
retained as an incident and carries no candidate or theorem load.

Even if accepted, this unit only extends candidate topology; the remaining
beta interval and the sign-to-`(H_tail)` relay remain separate obligations.

