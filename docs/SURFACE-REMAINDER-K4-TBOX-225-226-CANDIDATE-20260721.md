# K4 refined t-box `[9/4,113/50]`

The preregistered width-`1/100` refinement was run on
`delta=[1/25,81/2000]` with the frozen centred integrator, 9,216 cells, and
140 Arb bits. Production and replay are byte-identical and the validator
accepts all seven literal carrier fractions. The worst fraction is
`MD2r_mirror=0.260333467967994...`.

This result is candidate-only. It narrows the local boundary exposed by the
failed `[9/4,23/10]` box but supplies no global K4 delta/t cover, regular-ball
endpoint patch, overlap theorem, or weighted S1'''/S2''' union.
