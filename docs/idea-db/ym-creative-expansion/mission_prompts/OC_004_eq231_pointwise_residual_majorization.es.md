# Mission prompt — OC_004_eq231_pointwise_residual_majorization

    Trabajas en `lluiseriksson/THE-ERIKSSON-PROGRAMME`.

    ## Proof card

    `proof.eq231.pointwise-p-residual-majorization`

    ## Misión única

    After P dictionary, identify the pointwise residual term and factor it into amplitude times Eq231 geometry weight.

    ## Source keys permitidas en el primer pase

    - `cmp116.eq231.p-bond-sum`
- `cmp116.eq231.p-family-carrier-source-target`

    ## Target shape

    ```text
    pResidualWeight <= amplitude * pGeometryWeight and pGeometryWeight <= cmp116Eq231PWeight rate gapMass pBonds.
    ```

    ## Éxito mínimo

    - source residual symbol identified
- rate rho and bracket fields are explicit
- does not claim entropy alone closes residual

    ## Rechazar si

    - P dictionary is unresolved and silently assumed
- pointwise residual is replaced by final summed bound

    ## Output obligatorio

    1. Fuente exacta o blocker exacto.
    2. Declaración Lean propuesta o theorem real.
    3. Campo/hipótesis removida o reducida.
    4. Lista de hipótesis que siguen vivas.
    5. Evidencia de que no añadiste un consumidor cosmético.

    No añadas downstream wrappers salvo que un campo del contrato desaparezca o una cita se promocione con extracción real.
