# Sprint 15 — Eq. (2.31) three-field parallelization

Split the full P-family dictionary into three non-overlapping agent tasks:

1. `mem_iff_source`: exact Balaban family membership.
2. `source_subset_gapCarrier`: base-cube carrier ownership.
3. `admissible_iff_source`: Boolean reflection of source predicate.

Do not let agents solve one field by defining it in terms of another target.
The merger may assemble `cmp116Eq231_balabanPFamily_sourcePIndexMemIff` only after all three fields are real.
