# Sprint 02 — Eq. (2.37) fixed-Z0prime estimate

Objective: attack the fixed-Z0prime estimate consumed by `cmp116PostPResidualSourceBound_of_eq237`.

Output options:
- exact fixed-Z0prime theorem;
- final summation theorem;
- source-shaped package with both fields visible.

Do not merge the fixed estimate and final summation unless the source does.
