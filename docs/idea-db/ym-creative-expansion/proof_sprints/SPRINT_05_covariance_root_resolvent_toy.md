# Sprint 05 — Covariance-root resolvent toy

Objective: formalize finite weighted resolvent sums preserving nonnegativity and kernel decay.

Exit condition:
- a toy theorem proving decay inheritance for `Σ w_j R_j` with `w_j ≥ 0`;
- an explicit approximation-error slot.
