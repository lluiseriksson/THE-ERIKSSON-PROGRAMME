# Worksheet — Eq. (2.37) two-premise route

Extract only two source statements unless the paragraph explicitly supports more:

1. fixed-`Z0'` estimate feeding `heq237_fixed`;
2. final post-(2.37) summation feeding `hpost_eq237`.

Do not collapse `O(1)` constants.  Use a common-majorant theorem if needed.
