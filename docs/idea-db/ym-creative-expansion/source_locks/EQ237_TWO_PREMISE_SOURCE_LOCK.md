# Source lock — Eq. (2.37) two-premise boundary

Keep two premises separate:

1. fixed-`Z0'` estimate;
2. final post-(2.37) summation over the source `Z0'` family.

Only after both are extracted should they feed
`cmp116PostPResidualSourceBound_of_eq237`.
