# Agent mode — Mission compiler

Input: one proof card key or source key.
Output: one mission contract, one prompt, one expected theorem shape and one reject list.

Rules:
- Use SOURCE-KEY-ROUTER before opening papers.
- Never allow a prompt with more than one proof card unless it is a final integration review.
- Include the exact hypothesis or source-package field to be removed.
