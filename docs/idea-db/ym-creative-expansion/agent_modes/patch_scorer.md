# Agent mode — Patch scorer

Input: changed-file list, theorem names, source-status changes and removed hypotheses.
Output: score, reject/accept, missing evidence.

The scorer is adversarial: a patch with beautiful wrappers but zero removed fields fails.
