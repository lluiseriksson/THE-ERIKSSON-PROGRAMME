# Incident: lambda splitting does not rescue beta width 1/16

The preregistered beta cell [102,1633/16] was split into lambda cells
[3/2,17/10] and [17/10,19/10].  Both production attempts terminated with
“mean-value upper endpoint is not negative”; no replay certificates exist.

This retires the simple lambda-splitting repair for this beta width.  The
adjacent beta width-1/32 cell remains the largest passing configuration
currently recorded at this endpoint.  The failure is an enclosure-width
diagnostic, not a sign counterexample, and does not change G2/G6.
