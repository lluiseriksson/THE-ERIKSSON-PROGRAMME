# AREA-LAW VOLUME-UNIFORM PLAN (post-campaign refinement 2)

**Date:** 2026-06-12.  **Status:** design.  Upgrades the COMPLETE
finite-volume area laws (`docs/AREA-LAW-PLAN.md` linearized,
`docs/AREA-LAW-EXACT-PLAN.md` exact-activity) to a constant
independent of the lattice volume `#P`.

## 1. The honest statement of the gap

Both completed laws bound the UNNORMALIZED integral
`‖∫ tr(W_C)·∏_p(activity_p)‖` with a constant `2^{#P}` (crude subset
count) or `2^{#P}·e^{2δN_c·#P}` (exp-series tail).  The physical
object is the NORMALIZED Gibbs expectation `⟨tr W_C⟩_β = numerator/Z`,
and the physical claim is `‖⟨tr W_C⟩‖ ≤ C₀·r^{Area(C)}` with
`C₀ = C₀(N_c, d)` only.  The volume factor must cancel against `Z` —
this is the connected-support (polymer-ratio) resummation, and it is
genuinely campaign-scale.

## 2. Why the repo is unusually well positioned

The ENTIRE pipeline below is banked from the B2/B4 IR-clustering
campaign and the KP layer:

* polymer representation of `Z` and of deformed numerators
  (`L1_GibbsMeasure/PolymerRepresentation.lean`, `WeightedGas.lean`,
  `PolymerFactorization.lean`, Step-2 `Z = Ξ` reconstruction);
* Mayer–Ursell inversion `Ξ = exp(clusterSum)` (`KP/MayerInversion`);
* volume-uniform KP convergence with PINNED clusters
  (`KP/PinnedCluster`, `PinnedWalk`, `PinnedBound`, `SharpKP`,
  `ClusterTail`) — this is exactly the engine that made
  `truncated_correlation_bound` (B4) volume-uniform;
* the N-ality join + per-term dichotomy (`ClayCore/WilsonLoopMonomial`):
  every expansion term whose plaquette support cannot span `C`
  vanishes EXACTLY — this is what converts cluster-size decay into
  AREA decay.

What B4 did for the two-plaquette correlator (observable pinned at
`{p, q}`, clusters connecting them, KP tail in the distance), this
campaign does for the Wilson loop (observable pinned at `supp ∂C`,
clusters spanning the loop, KP tail at size `≥ Area`).

## 3. Brick ladder

| Brick | Content | Status |
|---|---|---|
| V0 | **Loop-tagged expansion of the numerator.** In the linearized class: `∫ tr(W_C)·∏(1+f_p) = ∑_{S ⊆ P} ∫ tr(W_C)·∏_{p∈S} f_p` (banked `integral_mul_prod_one_add`), regrouped by the connected components of `S ∪ supp(C)`: components DISJOINT from the loop's support factor out of the integral.  **V0-1 CLOSED** (`L1_GibbsMeasure/SupportFactorization.lean`, oracle clean): `DependsOnPos` (positive-edge-support dependence, with `mono`/`mul`/`finset_prod` calculus), `edgeSupport`/`plaquettePosSupport`, `dependsOnPos_comp_wilsonLine` (ONE lemma covers traces, linearized AND exponential activities via post-composition `φ : G → ℂ`), `integral_mul_of_disjoint_pos_deps` (two-block independence over `gaugeMeasureFrom`, transported along `gaugeConfigMEquiv`), `integral_mul_prod_of_disjoint_support` (the split headline) and its Wilson instantiation `integral_wilson_obs_mul_prod_split`.  House note: name `plaquettePosSupport` — `plaquetteSupport` is taken by `PolymerExpansion` at the signed-edge level.  REMAINING in V0 — **V0-2, the component regrouping** (designed 2026-06-12): partition each `S ⊆ P` of the powerset expansion as `S = S₀ ⊎ S_far` with `S₀` the union of the `plaqComponents` (banked in `ClusterGeometry.lean`: `plaqComponents_biUnion_eq`, `_support_disjoint`, `_not_touching`, `_isConnectedPolymer`, `_disjoint`) touching the loop's support and `S_far` the rest; then V0-1's `integral_mul_prod_of_disjoint_support` splits `∫ tr(W)·∏_S f = (∫ tr(W)·∏_{S₀} f)·(∫ ∏_{S_far} f)`.  **Named seam:** `ClusterGeometry` connectivity/touching lives at the SIGNED-edge level (`plaquetteSupport : Finset (ConcreteEdge d N)`, `PolymerExpansion`), while V0-1's independence is at the POSITIVE-edge level (`plaquettePosSupport : Finset (PosEdge d N)`); **The feared seam DISSOLVED** (V0-2 opening, CLOSED, oracle clean): `PolymerExpansion.plaquetteSupport` was already at the positive-edge level (`{(p.edges i).pos}`), so the bridge is an outright equality — `plaquettePosSupport_eq : plaquettePosSupport p = plaquetteSupport p` (ext + mem-level simp; house note: simp normalizes a 4-element list-map membership to a 4-way disjunction WITHOUT a trailing `∨ False`, so the rintro pattern is `(rfl \| rfl \| rfl \| rfl)`, not five cases) and `dependsOnPos_plaquette_obs'` states plaquette-observable dependence directly against `plaquetteSupport` — the exact interface `plaqComponents_support_disjoint`/`_not_touching` speak.  **V0-2 CLOSED** (oracle clean): `nearLoop es S` (the biUnion of the `plaqComponents` touching `edgeSupport es`) with `nearLoop_subset`, `mem_plaqComponents_of_mem`, `farLoop_disjoint_edgeSupport`, `near_far_support_disjoint` (different components ⇒ support-disjoint, via `plaqComponents_support_disjoint`), and the headline `integral_wilson_obs_regroup`: `∫ φ(W_C)·∏_{p∈S} f_p = (∫ φ(W_C)·∏_{nearLoop} f_p)·(∫ ∏_{S∖nearLoop} f_p)` for ANY activities with `DependsOnPos (f p) (plaquetteSupport p)`.  **V0 is COMPLETE** — next: V1, the ratio cancellation (sum the far factors back into `Z`-restricted form and control `Z`-ratios by the cluster expansion). | **V0 closed** |
| V1 | **Ratio cancellation.** Numerator `= (∑_{S₀ pinned to C} w_C(S₀)) · Z_{P∖nbhd}`-type factorization, and `Z_{P'} / Z_P` controlled by the KP cluster expansion (banked `Ξ = exp(clusterSum)` + volume-monotone cluster bounds).  Output: `⟨tr W_C⟩ = ∑_{S₀ pinned} w_C(S₀) · (Z-ratio)`, `|Z-ratio| ≤ e^{c·|∂-region|}` with `c` volume-independent.  **V1-a CLOSED** (oracle clean): `prod_one_add_eq_sum_powerset` (ℂ-valued binomial over arbitrary `F`) + `sum_integral_prod_eq_integral_prod_one_add` — the far resummation `∑_{T⊆F} ∫ ∏_T f = ∫ ∏_F (1+f)` (the restricted-`Z` object the ratio divides against).  NEXT (V1-b, the hard brick): the fiber bijection of the powerset sum — `S ↔ (S₀, T)` with `nearLoop es S₀ = S₀` pinned and `T ⊆ farRegion es S₀`.  **Forward half CLOSED** (oracle clean): `farRegion` (the index set of the far subsets) and `sdiff_nearLoop_subset_farRegion : S ∖ nearLoop es S ⊆ farRegion es (nearLoop es S)` — immediate from the V0-2 support lemmas since `plaquetteTouches = ¬Disjoint` of supports.  **Remaining (the genuinely hard half), step (1) CLOSED:** `mem_nearLoop_iff_reachable` — `p ∈ nearLoop es S ↔ ∃ q : ↥S, Reachable (fromRel plaquetteTouches) ⟨p⟩ q ∧ supp q meets the loop` (oracle clean).  House notes: the `biUnion id` wrapper leaves `id (B.image val)` in membership hypotheses — `simp only [id_eq]` before `rw [Finset.mem_image]`; and `Finpartition.mem_part_ofSetoid_iff_rel.mpr` cannot elaborate argument-first (its implicits stay metas) — ALWAYS ascribe the resulting membership in a `have` with the full `(Finpartition.ofSetoid …).part …` type; `Setoid.refl` needs an INSTANCE, use an ascribed `SimpleGraph.Reachable.refl` instead.  Steps (2) and (3) ALSO CLOSED (oracle clean): `walk_confined` — a `↥(S₀∪T)`-walk from a `T`-vertex stays in `T` under no cross-touching (`induction W with \| @cons u v w h W ih` to name the implicit vertices; `fromRel_adj` + `Disjoint.symm` per step); `reachable_union_of_reachable` — `S₀`-reachability transports along the inclusion hom via `Reachable.map` (house note: extracting `a = b` from an equality of subtype-IMAGES must go through an UNASCRIBED `have h2 := congrArg Subtype.val h` — any expected-type pressure re-routes the implicits to the wrong subtype).  Step (4) ALSO CLOSED (oracle clean): `reachable_descend` — a union-walk from an `S₀`-vertex stays in `S₀` and descends to `S₀`-reachability (converse induction; the per-step `S₀`-adjacency rebuilt with `fromRel_adj` + unascribed `congrArg Subtype.val` for the ne-transport).  **All four graph bricks of V1-b are banked.**  Step (5) ALSO CLOSED (oracle clean): `nearLoop_union_far` — **the stability theorem**: `nearLoop es (S₀ ∪ T) = S₀` for pinned `S₀` and `T ⊆ farRegion es S₀`.  The proof needed LESS than designed: ⊆ never descends a walk — the near-witness can't lie in `T` (far plaquettes avoid the loop), so a `T`-start is refuted by confinement alone and `p ∈ S₀` follows directly; ⊇ lifts the pinned witness.  (`reachable_descend` stays banked for the surjectivity half of the fiber map.)  House note: `hpin ▸ h` rewrites the WRONG direction here — use `by rw [hpin]; exact h`.  `nearLoop_idem` ALSO CLOSED (oracle clean), via `nearLoop_walk_descend` — a walk to a loop-toucher has ALL its vertices near, so it descends to the near part's own touching graph (induction with `\| @nil z` to name the generalized endpoint — referencing the statement's fixed variables inside `induction W` branches silently picks the WRONG vertex).  **V1-b IS COMPLETE** (oracle clean): `sum_powerset_fiber` — `∑_{S⊆P} g S = ∑_{S₀ pinned} ∑_{T⊆farRegion es S₀} g (S₀ ∪ T)`, via `Finset.sum_sigma'` + `sum_nbij'` with forward `S ↦ ⟨nearLoop es S, S∖nearLoop es S⟩` and inverse `⟨S₀,T⟩ ↦ S₀ ∪ T`; supporting `disjoint_farRegion` (self-touching: `plaquetteSupport_nonempty` + order-level `disjoint_self`, NOT `Finset.disjoint_self`).  House note: the `sum_nbij'` inverse-side goals carry unreduced beta-redexes `((fun S => …) ((fun x => …) p)).snd` — `rw` fails; insert a beta-reducing `show` first.  **V1-c ASSEMBLY CLOSED** (oracle clean): `integral_wilson_loop_tagged_expansion` — **THE LOOP-TAGGED EXPANSION**, `∫ φ(W_C)·∏_P(1+f_p) = ∑_{S₀ pinned} (∫ φ(W_C)·∏_{S₀} f_p)·Z_{farRegion(S₀)}` for any local activities (hypotheses: per-plaquette `DependsOnPos` + two integrability families).  Chained in one proof: pointwise binomial → `integral_finset_sum` → `sum_powerset_fiber` → per-fiber `integral_mul_prod_of_disjoint_support` → `sum_integral_prod_eq_integral_prod_one_add`.  Compiled FIRST TRY on the banked bricks.  ALSO CLOSED: `partition_loop_tagged_expansion` — the `φ = 1` instance: `Z = ∑_{S₀ pinned} (∫ ∏_{S₀} f)·Z_{farRegion(S₀)}` — numerator and denominator now fiber over the SAME pinned decomposition with the SAME far factors, the alignment the ratio cancellation divides along.  REMAINING in V1 — the `Z`-ratio bound, now mapped against the VERIFIED signatures (recon 2026-06-12): `KP.partition P Λ` already takes a polymer-volume `Λ` (`KP/Basic:71`), but `KP.clusterSum` (`KP/Expansion:44`) and the Mayer inversion `partition_eq_exp_clusterSum_of_kp` (`KP/MayerInversion:2038`) exist ONLY at `Λ = univ`; the lattice gate `partitionFunction_eq_partition` (`PolymerRepresentation:94`, the 3.2M-heartbeat `sum_nbij'`) is also univ-only.  Bricks: **R1 (abstract restriction) — PARTITION TRANSFER CLOSED** (oracle clean, core 8237, new module `KP/Restriction.lean`): `PolymerSystem.restrict P Λ` (Polymer := `↥Λ`, inherited incomp/activity) and `partition_restrict : partition P Λ = partition (P.restrict Λ) univ` (admissible-family `sum_nbij'` along `Finset.subtype`/`map (Embedding.subtype)`; house note: `sum_nbij'` branch goals carry beta-redexes AND `(P.restrict Λ).Polymer` structure-projection types — work in TERM style, `Finset.mem_*.mp/.mpr` apply through defeq where `rw` fails; `Finset.subtype_map` + `filter_true_of_mem` give the left inverse).  **R1 IS COMPLETE** (oracle clean): `KPCriterion.restrict` (the criterion's sums are sub-sums — `Finset.sum_map` with ALL arguments explicit, the `?f (e x)` HO-pattern does not unify with metas) and `partition_eq_exp_clusterSum_restrict : partition P Λ = exp(clusterSum (P.restrict Λ))` under the ambient KP criterion — **the volume-restricted Mayer inversion**, with zero new analysis.  The `Z`-ratio of two volumes is now literally `exp(clusterSum_Λ − clusterSum_Λ')`. **R2 (the difference)** — `clusterSum P − clusterSum (P.restrict Λ)` = sum over clusters MEETING `Λᶜ`; norm ≤ `#Λᶜ_polymers·(pinned tail)` via `PinnedBound`; ratio `|Z_Λ/Z| ≤ exp(c·#(loop nbhd))`, volume-free.  **R2 substrate CLOSED** (oracle clean): `incompGraph_restrict`, `ursell_restrict`, `prod_activity_restrict` — all three are `rfl` (the restriction inherits `incomp`/`activity` definitionally, and `ursell` reads only the incompatibility graph).  `clusterSum_restrict` ALSO CLOSED (oracle clean, first-try): `clusterSum (P.restrict Λ) = ∑'_n (1/(n+1)!)·∑_{X : tuples valued in Λ} ursell·∏z` — the dependent `Finset.sum_bij'` with the value-tuple maps; ALL five side goals are one-liners because the R2 substrate transports are `rfl`.  **R2(a) ALSO CLOSED** (oracle clean): `clusterSum_sub_restrict` — under the KP criterion, `clusterSum P − clusterSum (P.restrict Λ) = ∑'_n (1/(n+1)!)·∑_{X : ¬∀i, Xᵢ∈Λ} ursell·∏z`, via per-`n` `clusterTerm_restrict` (the refactored dependent bijection), summability of both series from `norm_clusterTerm_le` + `kp_clusterWeight_summable_sharp` (the restricted one through `KPCriterion.restrict` — the R1 pieces snapping together), `Summable.tsum_sub` (namespaced, as the house rule predicts), and `sum_filter_add_sum_filter_not`.  Combined with R1: `partition P Λ / partition P univ = exp(−(that difference))` whenever `partition P univ ≠ 0`.  **R2(b1–b2) ALSO CLOSED** (oracle clean): `offRegionClusterWeight` (the meeting-`Λᶜ` part of the per-size weight, with nonneg + `≤ clusterWeight` comparison), `norm_diffTerm_le` (term-wise majorant), and `norm_clusterSum_sub_restrict_le` — **the `Z`-ratio exponent bound**: `‖clusterSum P − clusterSum (P.restrict Λ)‖ ≤ ∑'_n offRegionClusterWeight P Λ n` under the KP criterion.  **R2(b3) ALSO CLOSED** (oracle clean): `offRegionClusterWeight_le_pinned` — `offRegionClusterWeight P Λ n ≤ (n+1)·∑_{c ∉ Λ} pinnedClusterWeight P c n`, by the per-tuple index-indicator bound (`single_le_sum` at the escaping index), `sum_comm`, the swap reindex per index (`sum_nbij'` with `· ∘ Equiv.swap 0 i`; `ursell_comp_equiv` + `Equiv.prod_comp` give the `f`-invariance), and the `Y₀`-fiberwise decomposition onto `Λᶜ` (`sum_fiberwise_of_maps_to` + `filter_filter` congr).  House notes: `split_ifs` on `if X i ∉ Λ` cases on the UNNEGATED condition — the bullets come in the OPPOSITE order to the ite branches; beta-redexes from `Finset.sum_nbij'`/`filter_congr` binders again need `show` before `rw`/`split_ifs`.  LAST R2 step — (b4) sum over `n`, ROUTE FIXED + substrate closed: the `(n+1)` factor is absorbed by the **scalar activity tilt** — `PolymerSystem.scaleActivity P r` (CLOSED, oracle clean, with `pinnedClusterWeight_scale : pinned_{scale r}(c,n) = |r|^{n+1}·pinned(c,n)`; house note: `rw` with a sum-equality fails when the goal's sum is typed over `(P.scaleActivity r).Polymer` — defeq but not syntactic — close with `congrArg (inv * ·) hsum |>.trans (by ring)`).  Since `y ≤ e^y` gives `(n+1) ≤ t⁻¹·e^{t(n+1)}`, the b3 bound yields `∑'_n offRegion ≤ t⁻¹·∑_{c∉Λ} ∑'_n pinned_{scale e^t}(c,n) ≤ t⁻¹·∑_{c∉Λ} e^t·‖z c‖·e^{a c}` by `pinned_cluster_summable_sharp` applied to the TILTED system — so b4's final statement takes `KPCriterion (P.scaleActivity (exp t)) a` as input (exactly what the lattice side verifies: its KP criterion holds WITH tilt room, `weighted_unitTilt_kpCriterion_volumeUniform`).  **R2 IS COMPLETE** (oracle clean): `tsum_offRegionClusterWeight_le` — under `KPCriterion (P.scaleActivity (exp t)) a`, `∑'_n offRegion(n) ≤ t⁻¹·∑_{c∉Λ} e^t·‖z(c)‖·e^{a(c)}` — **the volume-free `Z`-ratio exponent bound**, chaining b3 + the scale transport + `y ≤ e^y` + the sharp pinned tails at the tilted system.  House note: even with `(s :=)/(f :=)` named args, `rw` with `Summable.tsum_finsetSum` leaves a beta-redex pattern that won't match — use `le_trans (le_of_eq hswap) ?_` instead; and a closing `rw` chain that lands on rfl CLOSES the goal — a trailing `ring` then errors `No goals`.  **The abstract `Z`-ratio theory (R1+R2) is DONE:** `‖log(Z_Λ/Z)‖ ≤ t⁻¹·∑_{c∉Λ} e^t‖z(c)‖e^{a(c)}`.  Remaining in V1: only R3 — and its route is now the TRUNCATION DEVICE, *not* a re-run of the heavy bijection: the region-restricted lattice `Z_F = ∫∏_{p∈F}(1+w_p)` equals the FULL weighted partition at the truncated weight `w' := w·1_F` (factors outside `F` are `1+0`; `IsLocalWeight`/measurability/bound trivially inherited), so the banked `weightedPartition_eq_partition` (`PolymerRepresentation:335`, the 3.2M-heartbeat gate, REAL weight families) applies UNCHANGED, and the truncated gas collapses to the `F`-polymers by the new abstract dropper `partition_eq_of_activity_eq_zero` (**CLOSED**, oracle clean): activities vanishing off `Λ` ⇒ `partition P univ = partition P Λ` (`Finset.sum_subset` + `prod_eq_zero`).  **R3 IS COMPLETE** (oracle clean, `L1_GibbsMeasure/RestrictedGate.lean`, core 8238): `truncWeight` substrate (steps i–ii) and the assembly `restricted_weightedPartition_eq_partition` — **`∫∏_{p∈F}(1+w_p) = Ξ_{gas(w)}(polymersIn F)`** — by truncate → banked gate → drop vanished activities → restore original activities (`Admissible` literally shared since incomp is w-independent; the final congruence is two `sum/prod_congr`s).  NOTE: REAL weight families (the physical `Re tr` observable); exact-ℂ is the V4 stretch.  **WITH R1+R2+R3 THE `Z`-RATIO MACHINERY OF V1 IS COMPLETE.**  What remains to CLOSE V1 outright: the glue — instantiate `Λ := polymersIn (farRegion es S₀)` in `partition_eq_exp_clusterSum_restrict` + `tsum_offRegionClusterWeight_le`, and bound the `Λᶜ`-pinned sums by the loop-neighbourhood geometry (`ConnectedEntropy` counting).  **Glue step 1 CLOSED** (oracle clean): `KPCriterion.of_activity_norm_le` — the KP criterion is monotone in activity norms over a fixed carrier (stated via the structure-update `{ P with activity := z }`, so BOTH the banked double-tilt `(gas.tilt card).tilt (ε·card)` (`weighted_unitTilt_kpCriterion_volumeUniform`) and the target `gas.scaleActivity (exp s)` are literally updates of the same gas, and `e^s‖z‖ ≤ e^{(1+ε)|c|}‖z‖` for `s ≤ 1+ε`, `|c| ≥ 1` transfers the criterion).  **Glue step 2 ALSO CLOSED** (oracle clean): `weighted_scale_kpCriterion` — the lattice gas satisfies `KPCriterion (gas.scaleActivity (exp s)) (t·card)` for `s ≤ 1+ε` under the banked numeric hypotheses, by `of_activity_norm_le` against the double-tilt criterion (house note: annotate the `z₂`-lambda's binder TYPE — projections `c.1` on an inferred-meta binder fail with "Invalid projection").  **EVERYTHING `tsum_offRegionClusterWeight_le` needs at the lattice is now in place.**  Glue step 3 (final, est. ½–1 session): the instantiated ratio statement — combine `restricted_weightedPartition_eq_partition` (Z_F as restricted Ξ), `partition_eq_exp_clusterSum_restrict` (its log), `clusterSum_sub_restrict` + `norm_clusterSum_sub_restrict_le` + `tsum_offRegionClusterWeight_le` at `Λ := polymersIn (farRegion es S₀)`, `a := t·card`, `s := 1` into: `Z ≠ 0`, `Z_F/Z = exp(−D)` with `‖D‖ ≤ t⁻¹·∑_{c : c.1 ⊄ F} e·‖z c‖·e^{t·|c|}` — then the neighbourhood count (polymers NOT inside `F` must MEET `P∖F = nearLoop-region`; `ConnectedEntropy`) renders it `≤ C(d,δ,t)·#(P∖F)`.  Then V2. | (updated) | **R3 (lattice instantiation)** — parametrize `partitionFunction_eq_partition` by a plaquette region `F` (the `componentFamily` bijection restricts: components of `S ⊆ F` lie in `F`), giving `∫∏_F(1+f) = KP.partition gas (polymersIn F)`; instantiate `Λ := polymersIn (farRegion es S₀)`.  Est. 2–3 sessions; then V2 (the pinned area tail) closes the campaign. | **V1-a,b + V1-c(assembly) closed** |
| V2 | **The pinned area tail.**  REFINED DESIGN (post-V1): normalize the tagged expansion — `⟨φ(W)⟩ = ∑_{S₀ pinned} (∫φW∏_{S₀}f)·(Z_far(S₀)/Z)`; the ratio factor is `≤ e^{C·#(P∖farRegion(S₀))}` (V1) with `#(P∖farRegion(S₀)) ≤ κ(d)·(|C| + |S₀|)` (each excluded plaquette touches the loop or `S₀` — lattice coordination count, NEW small lemma).  KILL: `∫φW∏_{S₀}f = 0` for `|S₀| < Area` (banked join).  Survivors: `‖∫φW∏_{S₀}f‖ ≤ N_c·(2δN_c)^{|S₀|}` (banked dichotomy/AL6 bounds); pull out `r^{Area}`, absorb `e^{Cκ|S₀|}` into the geometric rate.  TAIL — the honest entropy: pinned `S₀` are DISJOINT UNIONS of loop-anchored components, so resum as a polymer gas over loop-touching components — `∑_{S₀} ∏ ≤ exp(∑_{single loop-touching components} weight) ≤ e^{c·κ|C|}` (the through-plaquette sums, banked) — giving the headline in PERIMETER-PREFACTOR form `‖⟨Re tr W_C⟩‖ ≤ N_c·e^{c(d,δ,t)·|C|}·r^{Area(C)}` (the `|C|`-linear prefactor of the plan's §3 was optimistic; `e^{c|C|}` is the standard honest form).  **V2-0 CLOSED** (oracle clean): `integral_prod_one_add_ofReal` — the ℝ↔ℂ bridge identifying the tagged expansion's far factors with the casts of the real restricted `Z`s (the form the V1 ratio bounds speak about).  **V2-1 ALSO CLOSED** (oracle clean): `card_compl_farRegion_le` — `#(farRegion es S₀)ᶜ ≤ #edgeSupport(es)·4d + #S₀·16d`, charging each excluded plaquette to a loop edge it crosses (`card_plaquettesThroughEdge_le`, banked) or a pinned plaquette it touches (`card_plaquettesTouching_le`, banked), via biUnion subset + `card_biUnion_le`.  **V2-2 ALSO CLOSED** (oracle clean): `norm_integral_pinned_term_le` (`WilsonLoopMonomial.lean`) — the pinned-term dichotomy in SET form, `‖∫ tr(W)·∏_{p∈S₀} f_p‖ ≤ ite (Area ≤ #S₀) (N_c·(2δN_c)^{#S₀}) 0` — by instantiating the per-multiplicity dichotomy at the INDICATOR `m := 1_{S₀}` (the `∏_p z^{m p}/m p!` collapses to `∏_{S₀} z`; house note: TWO paren-balance traps — a bare `∏ p, … = …` swallows the equality into the binder, and a `(fun A : T => …)`-wrapped show-lambda needs its own close; symptom is `CommMonoid Prop` or `unexpected 'from'`).  Kill AND survivor bound in one banked stroke.  Remaining: V2-3 — the final assembly: normalize the tagged expansion by `Z` (nonvanishing from `Z = exp(clusterSum)` via `Complex.exp_ne_zero`), apply V2-2 per pinned term + the V1 ratio with the V2-1 count, pull `r^{Area}` out, and resum the loop-anchored component gas (`e^{c·|C|}` prefactor) ⇒ `‖⟨Re tr W_C⟩‖ ≤ N_c·e^{c(d,δ,t)·|C|}·r^{Area}`.  **V2-3 substrate opened** (oracle clean): `norm_exp_div_norm_exp_le : ‖a−b‖ ≤ C → ‖exp b‖/‖exp a‖ ≤ e^C` — the exact shape in which the V1 cluster-difference bound converts each pinned term's `‖Z_far‖/‖Z‖` into `e^{C·#nbhd}` inside the sum.  **V2-3a CLOSED** (oracle clean, `WilsonLoopMonomial.lean`): `norm_integral_wilson_loop_le_pinned_sum` — `‖∫ tr(W_C)·∏_P(1+f)‖ ≤ ∑_{S₀ pinned} ite(Area ≤ #S₀)(N_c(2δN_c)^{#S₀})(0)·‖Z_far(S₀)‖`, chaining the tagged expansion (instantiated at SU(N_c) — house notes: needed `maxHeartbeats 1600000` for the instantiation's defeq checks, the binder-annotated `φ`-lambda, and the `le_of_eq (congrArg norm heq)` defeq-tolerant entry instead of `rw`) + `norm_sum_le` + the V2-2 set dichotomy per term.  THE KILL IS WIRED INTO THE NORMALIZED EXPANSION.  **V2-3b mechanics ALSO CLOSED** (oracle clean): `norm_div_le_pinned_sum_exp` — from `‖Num‖ ≤ ∑ a_i·‖exp ζf_i‖`, `Z = exp ζ`, and `‖ζ−ζf_i‖ ≤ C_i`, conclude `‖Num/Z‖ ≤ ∑ a_i·e^{C_i}` (pure algebra: `div_le_div_iff_of_pos_right`, `sum_div`, the exp-ratio lemma per term).  Remaining — the LAST two pieces of the whole campaign: (3b') the lattice instantiation feeding 3b's hypotheses — REAL activities (`f_p` real-valued, e.g. `c' = conj c`) so the V1 gate gives `Z = exp(clusterSum gas)` and `Z_far(S₀) = exp(clusterSum restrict)` via the ℝ↔ℂ bridge, with `C_{S₀} := (#loopSupp·4d + #S₀·16d)·C(d,δ,t)` from V1+V2-1 — **3b′(i) CLOSED** (oracle clean): `wilsonLine_congr_of_configToPos_eq` (config-level Wilson-line locality via the banked `config_eval_eq_of_pos`) and `isLocalWeight_reActivity` — the real linearized activity `w_p = 2·Re(c_p·tr H_p)` IS a local weight, the gate's interface (house note: the SU measurable-space instances live in `P8_PhysicalGap.SUN_StateConstruction` — import it explicitly; `IsLocalWeight` goals carry applied-lambda beta-redexes, enter with `show`); **3b′(ii) ALSO CLOSED** (oracle clean): `measurable_reActivity`, `reActivity_bound` (`|w_p| ≤ 2δN_c`), and `one_add_conjPair_eq_cast` — at `c' = conj c` the ℂ-activity factor `1 + f_p` IS the cast of `1 + w_p` (`Complex.add_conj`) — **ALL of the restricted gate's hypotheses are now discharged for the physical activity**, and the V2-3a far factors identify with the casts of the real restricted `Z`s via `integral_prod_one_add_ofReal` + pointwise congr.  The remaining 3b′ work is one composition: feed `restricted_partition_log_ratio_bound` + `card_compl_farRegion_le` + `norm_div_le_pinned_sum_exp` into V2-3a at the conjugate pair; (3c) the tail: split `a_{S₀}·e^{C_{S₀}}` as `e^{c₁|C|}·(N_c·(2δN_c·e^{c₂})^{#S₀})` on survivors, pull `r^{Area}` out, resum the loop-anchored component gas. | V2-0,1,2,3a,3b closed |
| V3 | **Assembly:** `area_law_volume_uniform` — `‖⟨tr W_C⟩_β‖ ≤ C₀(N_c, d)·|C|·r^{Area(C)}`, `r = ν·N_c·δ < 1`, all constants volume-free. | open |
| V4 | *(stretch)* The exact-activity version via the E4b dichotomy in place of the linear join. | open |

## 4. Honest difficulties (named, not hidden)

* **D1 — the loop is not a polymer.**  `tr(W_C)` couples ALL edges of
  `C` simultaneously; the component regrouping in V0 must treat
  `supp(C)` as a single pinned vertex.  The B4 analogue treated TWO
  pinned plaquettes; the loop needs the general pinned-set version of
  `PinnedCluster`.  Mechanical but large.
* **D2 — connectivity vs the join.**  The join kills `|S| < Area`
  REGARDLESS of connectivity; but after ratio cancellation the
  surviving sum runs over CONNECTED-to-C supports only.  The kill
  applies verbatim (a subset of the killed terms) — no new
  mathematics, but the bookkeeping of "connected component touching
  supp(C)" needs a clean Finset-graph formulation (banked
  `ClusterGeometry`).
* **D3 — the Z-ratio.**  `Z` restricted to the complement is not
  literally a factor of `Z`; the standard fix is the cluster-expansion
  log-ratio bound.  The repo's `Ξ = exp(clusterSum)` + `KPBound` give
  exactly this, but wiring it to the gauge-marginal `Z` (not the
  abstract gas) is V1's real content.

## 5. Estimated effort

V0: 2–3 sessions (component regrouping over the gauge integral).
V1: 2–4 sessions (the campaign's center of mass — D3).
V2: 1–2 sessions (entropy + tail, heavily banked).
V3: 1 session.  Total 6–10 sessions — comparable to B4.

## 6. What this does not promise

Volume-uniform AREA decay of the lattice Wilson loop at strong
coupling, M3-side (Osterwalder–Seiler regime).  No continuum, no OS
reconstruction, no statement at weak coupling (the physical
confinement regime for d=4 SU(3) continuum — open).  Distance to
Clay: ~0% (<0.1%), unchanged.
