# K4 t-box [2.970,2.975] boundary failure — 2026-07-26

The generic endpoint contract rejects `t=[2.97,2.975]` on the fixed delta
strip.  Production and replay are byte-identical; the only aggregate row
above budget is

```text
nuD_main = [1.03914976867858 +/- 3.10e-15]
```

The failed pair is retained in
`scripts/surface_remainder_k4_tbox_2970_2975.txt` and its replay.  No positive
manifest is generated.  The next permitted repair is an explicit width
`0.0025` subdivision; this boundary also shows the increasing partition cost
near the endpoint.
