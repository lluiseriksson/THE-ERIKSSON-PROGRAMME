# Terminal rescue-300 direct-sign attempt for the post-1635/16 gap

This is a fresh terminal attempt for the remaining finite-beta interval
`[1635/16,1000/9]`.  The first probe box is `[1635/16,3271/32]` (width
`1/32`); it uses the frozen rescue-300 backend with CWIN `3/2`, beta order
`40`, t order `50`, Arb precision `300`, and minimum t width `1/100000`.

Acceptance is strict: every recursive t row must have a negative outward
upper endpoint and production/replay must be byte-identical with current
dependency hashes.  This is direct `W^J<0` evidence only and does not by
itself promote `(H_tail)`, G2, or G6.
