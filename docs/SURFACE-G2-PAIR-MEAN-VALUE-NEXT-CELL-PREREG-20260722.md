# Preregistration: next dyadic pair mean-value cell

Before reading its result, register the next adjacent cell in the same
candidate route:

```text
beta  [13058/128,13059/128]
lambda [3/2,19/10]
modes 115; beta/lambda orders 50/50; Arb 500 bits
```

The frozen runner must produce a production/replay pair with identical rows,
dependency hashes, and a strict negative `total_upper`.  A timeout or
nonnegative enclosure is a retained diagnostic.  A pass is one
`CELL_CERTIFIED_CANDIDATE` only; it cannot promote G2/G6 or repair the global
finite-beta relay.
