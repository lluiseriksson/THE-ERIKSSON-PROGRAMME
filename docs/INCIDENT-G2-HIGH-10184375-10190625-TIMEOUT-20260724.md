# Incident: high-order finite-beta unit timeout `[3259/32,16305/160]`

**Date:** 2026-07-24  
**Scope:** candidate finite-beta scaled-bulk sign lane only; no G2, K2,
`(H_tail)`, or G6 promotion.

## Reproduction

The generic CWIN=`3/2`, order-30, `t_order=37`, Arb-180 driver was launched
with:

```text
python scripts/run_surface_scaled_bulk_cwin3p2_high_unit.py \
  --unit cwin3p2_high_101p84375_101p90625_codex \
  --lo 3259/32 --hi 16305/160
```

The adaptive `t` subdivision did not emit a production transcript within the
510-second execution window.  No replay, manifest, row count, or sign result
was generated.  The process exited without a terminal transcript; therefore
the interval remains an uncovered candidate gap rather than a numerical pass
or disproof.

A narrower retry with width `1/256`,
`[3259/32,26073/256]`, using the same driver and precision, also reached the
300-second command limit without producing a transcript.  This confirms that
the cost is not cured by merely shrinking the beta width.

An exploratory order-12 / `t_order=9` run on the same 1/256 cell also timed
out after 120 seconds.  It was not written as evidence because the lower
orders have no pre-registered sign contract for this seam.

## Consequence

`audit_surface_finite_beta_bridge_candidate_coverage.py` must continue to
report the upper component as incomplete.  This incident does not change the
G2 state (`REGULAR_008_AND_HYBRID_009_CERTIFIED` with
`RELAY_LEMMA_UNPROVED`) and does not authorize any manuscript-slot removal.

Any retry must be a newly preregistered subdivision or a different,
independently justified enclosure route; increasing the timeout alone is not
evidence.
