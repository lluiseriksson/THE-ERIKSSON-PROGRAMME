# Independent SU2 audit — PR 35 at `ecbd5c68`

Snapshot:

```text
producer: ecbd5c6831f982526602433e3fface0ab964d501
tree:     79a56723c13edd08f3cd5fbea0318bdc0356449d
PR:       https://github.com/lluiseriksson/THE-ERIKSSON-PROGRAMME/pull/35
```

## Mandatory raw freshness seal

```text
UTC=2026-07-31T08:51:10Z
COMMAND=git ls-remote origin refs/heads/main refs/pull/34/head refs/pull/35/head refs/pull/36/head refs/pull/37/head
5683f5929b0d4e2f0114bb8bac73ad39131ad4b7	refs/heads/main
0a46e266fc4808332ed20d2ab4611bfc271b208b	refs/pull/34/head
ecbd5c6831f982526602433e3fface0ab964d501	refs/pull/35/head
7fe64bbced729337f6a1060d731e661384863c42	refs/pull/36/head
e63f1847b2e8a91631ec7f8ff7318a338534e2b0	refs/pull/37/head
COMMAND=git rev-list --left-right --count ecbd5c6831f982526602433e3fface0ab964d501...refs/remotes/audit/pr35
0	0
```

This report supersedes the verdict at `581f58f7` for the moving PR head. It
does not rewrite the historical result at that SHA. The final move
`4b82c764 -> ecbd5c68` changed only `docs/su2-os/AUDIT.md`, adding the
owner's parked-state/no-retry policy. All Lean blobs and the sharp-gate
mathematics are byte-identical to the clean build at `4b82c764`.

## Preregistered question and verdict

The single preregistered question was:

> Do the new “degree-one pairing” and “sharp gate” commits prove the physical
> `GaugeConfig` gluing, or do they refine the autonomous one-plaquette
> endpoint?

**Answer: branch (B), endpoint refinement.** The bilateral prediction in the
`581f58f7` audit is fulfilled.

The two new commits prove a substantive result:

```text
Q_beta(trace) >= beta / 4
```

for the exact SU(2) crossing kernel and transport it to the already declared
one-plaquette reflected pairing. The degree-zero Haar mode vanishes, Schur
orthogonality gives the two diagonal moments `1/2`, the degree-one form is
exactly `beta/4`, every degree-at-least-two finite tail is PSD, and the
existing dominated-convergence theorem passes the lower bound to the exact
exponential kernel.

They do **not** add a map from repository `GaugeConfig`, an inverse assembly
map, a Haar pushforward theorem, an equality with
`GaugeConfig.plaquetteHolonomy`/`wilsonAction`, or a count of all plaquettes
crossing a physical reflection plane. The producer now states this boundary
explicitly: the geometry is a one-transporter temporal-gauge cut, the
transporter cancels, and a plaquette
`x c1 y^-1 c2^-1` with two independent transporters is not covered.

Accordingly:

- **PASS, scoped:** the sharp zero-mean character gate for the autonomous
  one-plaquette model;
- **BLOCKED:** `SU2-2G` / `CLAIM-SU2-1`, the physical lattice gluing claim.

No merge is performed.

## Sharp-chain audit

### Haar and Schur normalization — PASS

`su2TraceObservable` is the unnormalised fundamental `2 x 2` trace.
`su2TraceEntryMoment_eq` proves

```text
integral conj(tr U) U_ij dHaar(U) = delta_ij / 2
```

on the probability Haar measure `sunHaarProb 2`. There are exactly two
nonzero diagonal moments. With the exponent coefficient `beta/2`,

```text
(beta/2) * (|1/2|^2 + |1/2|^2) = beta/4.
```

No representation-dimension factor is missing.

### Conjugation and complex pairing — PASS in the declared model

`su2WilsonExponentKernel_eq_entry_sum` has the orientation
`x_ij * conj(y_ij)`. `kernelIntegralForm_entry_eq` therefore factors each
rank-one term as
`(beta/2) M_ij * conj(M_ij)`, where
`M_ij = integral conj(F(U)) U_ij dHaar(U)`. This matches the outer
`star(F x)` convention. The trace is real on SU(2), but the proof still
establishes the complex quadratic form rather than only a real slice.

### Tail and exact-kernel passage — PASS

The constant mode is zero for `F=tr`. The two-term Taylor head is exactly
`beta/4`. `su2WilsonTaylorTailKernel` begins at degree `n+2`; its finite
positive presentation gives a nonnegative Haar form for every truncation.
`su2Trace_taylor_lower` bounds every shifted truncation, and
`su2Trace_crossing_lower` uses the already proved convergence of the integral
forms to retain the closed lower bound. The fixed gate was not weakened.

### Reflected endpoint — PASS in the declared model

`su2OnePlaquette_trace_reflection_positive_sharp` combines the sharp kernel
bound with `su2OnePlaquetteReflectedPairing_eq_kernelIntegralForm`. It returns
a real `r >= beta/4 > 0` when `beta>0`.

### Physical lattice gluing — BLOCKED, unchanged

Repository-wide search finds `GaugeConfig` only in the producer's audit prose,
where the missing bridge is acknowledged. The load-bearing Lean declarations
still use:

```text
SU2CutConfig Half Cross =
  (Half -> SU2) x (Cross -> SU2) x (Half -> SU2)
SU2OnePlaquetteCut = SU2CutConfig Unit Unit
```

The `Cross` variable is inverted and integrated, but the same transporter
dresses both sides and cancels. This is an exact theorem for a product model,
not a theorem that a physical lattice configuration space has that product
decomposition. The five missing items from the preceding audit remain:

1. physical link/plaquette sets and reflection plane;
2. full-configuration assembly map and inverse;
3. product-Haar pushforward;
4. equality with the repository Wilson action including every crossing term;
5. exact boundary, fixed-edge, and crossing-plaquette multiplicities.

## Clean-checkout evidence

A new public clone was checked out detached at the full producer SHA; tracked
status was clean. A warm dependency cache was mounted only for pinned
dependencies.

```text
lake build YangMills.OS.SU2WilsonReflectionEndpoint
exit 0
Build completed successfully (8182 jobs).
elapsed 119829 ms
```

The producer oracle exited `0` and all sixteen queries printed only:

```text
[propext, Classical.choice, Quot.sound]
```

The independent oracle is
`oracles/SU2-PR35-ecbd5c68-oracle.lean`.
It completed all nine independent queries with the same axiom list and empty
stderr. All four owned module-prose checks, the consistency scan, source-db
verification, and `git diff --check` passed; the owned-source
`axiom`/`sorry` declaration scan returned no matches.
Independent-oracle SHA-256:
`4AB976D171F2F97065F3BA56D311D5BD465E80B91E7E770C1EC7A50C868FEF85`.

Pinned inputs:

| Input | SHA-256 |
|---|---|
| `lean-toolchain` | `8C46C0308E92095E478BCFAE7C357327E88C5A624B54ABF5AD1660EE0E51DF5A` |
| `lake-manifest.json` | `E2F2D45A5FEF5AE352E6F8BE858726D603D83FDE30D740A14A8A2A588579381D` |
| `lakefile.lean` | `09D3FF29B030A20C396CDD5F729230EEB7BCDE3AE91CDA519C0643AC6B715BD5` |

Load-bearing sources:

| File | Git blob | SHA-256 |
|---|---|---|
| `SU2WilsonReflectionKernel.lean` | `e82ecfb005baabe5dd17f247e6d82175582cf048` | `DF11637DC7837C9D4FEC141F59D1DE6AAAB12395FCEE14423A060337D5EAAEE8` |
| `SU2WilsonReflectionGeometry.lean` | `4a916c9d0115d4d70806270e685cf0f9d2f47843` | `3031605BB3CD122AAD0008ECFAF78315962719530968B46DA934C4A7DAC55361` |
| `SU2WilsonReflectionSharp.lean` | `354d1fd1b7e824968398854a299dcc9d1df5e0e5` | `D5645AD6C766B33C65E8282FEF62819B45E28C0370E1EC698E6907B7109F069C` |
| `SU2WilsonReflectionEndpoint.lean` | `6a11281a2132267c1fe94987eb722cf49aba5055` | `5C0761FA6FB479DF4F5F87CA47A2C4CC5363A7175DF9556560B5E7C8E74915AE` |
| `SU2OSOracle.lean` | `6dea7ae52c2c01d02600e244ffabb403120902f4` | `C0FAAE3B15B1D4E2A5F81AB1BE4CCAC805DBB029451949BD7A0F0E983817C11A` |

## Independent-model disclosure

The one permitted Fable High request had already returned HTTP 429 with
`is_error=true`; it was rejected and not retried. Exact `claude-opus-5`
attempts produced no acceptable JSON. Neither model contributed evidence or
a conclusion.

## Final status

Manifest: `runs/SU2-PR35-ecbd5c68.json`.

```text
PASS:    6
FAIL:    0
BLOCKED: 5
```

The sharp gate is closed. The physical `GaugeConfig` gluing gate is not.
