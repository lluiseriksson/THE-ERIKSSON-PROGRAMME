# Preregistration — 300-bit rescue box `[3409/32,1705/16]`

Registered before execution: 2026-07-26  
Scope: one finite-beta candidate unit; no automatic G2/G6 promotion.

## Frozen contract

The unit is

```text
beta_lo = 3409/32
beta_hi = 1705/16
CWIN = 3/2
beta Taylor order = 40
t Taylor order = 50
Arb precision = 300 bits
minimum t width = 1/100000
```

The production driver is
`scripts/run_surface_scaled_bulk_cwin3p2_rescue300.py`, with unit name
`gap106p53125_106p5625_r300prereg`.  It must emit a contiguous cover of
`t=[3/5, pi_upper-CWIN/beta_hi]`, every row must have outward-rounded strict
upper endpoint `<0`, and the transcript must print the dependency hashes.

The replay is the identical command with `--replay`.  Acceptance requires
both transcripts to pass
`scripts/validate_surface_scaled_bulk_cwin3p2_rescue300.py`, to be byte equal,
and to contain no failed or partial output.  A failure or timeout is recorded
as an incident and is not deleted or converted into a candidate manifest.

Even on success this unit remains candidate-only until the complete residual
beta interval `[3409/32,1000/9]` has an exhaustive preregistered cover and the
independent sign-to-`(H_tail)` role/relay audit passes.

