# Incident: beta order 60 does not rescue width 1/8

The preregistered cell beta=[102,817/8] with lambda=[3/2,19/10],
modes 115, beta order 60, lambda order 50, and 500 Arb bits terminated with
“mean-value upper endpoint is not negative”.  Only the retained failed
transcript exists; no replay certificate was run.

The same order-60 backend passes beta width 1/16, so the failure is an
enclosure-width diagnostic rather than a sign counterexample.  Width 1/8 is
retired at this endpoint; no gate or manuscript state changes.
