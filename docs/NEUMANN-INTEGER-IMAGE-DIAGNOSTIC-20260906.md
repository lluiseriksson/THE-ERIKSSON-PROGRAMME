# Integer image dictionary diagnostic (prepared; NOT compiler-verified)

Source checkpoint9bba6c7b8c6f447a8e1914acca66e5c904beb154.
Instrument checkpointf429e5b8007e666f1d19dad73715f9176e031ac1.
The source-importing draft has seven names. Three division proof bodies were
previously HOT-verified (ledger1131); no new composite proof is validated yet.

Order: pinned bootstrap/cache -> exact Mathlib extraction/repro (120s maximum)
-> existing printed-image module prerequisites -> actual seven-name draft
(120s maximum) -> unchanged tracked sources -> independent archive verifier.
Stop on first child error. Never count the extracted declarations as an
actual project compilation. Extracted proof statements/bodies are verbatim;
only the imported image definitions are copied from the three pinned Git blobs.
Exact generated repro SHA256b13320f24c96e5c042e7e95676074076a04d32290cd1244a3ceeb152de6a16a5.

Instrument self-test: one synthetic accepted fixture and eleven deliberately
corrupted fixtures rejected. Exit0/0.3562274s,20135936observed peak bytes,
30s/512MiB watchdog, no local Lean/Lake, no pool. All source hashes were read
from binary `git cat-file blob` outputs, not CRLF worktree files.

Colab only: CPU/highRAM>=40GiB, no GPU, lluiseriksson@gmail.com.
One notebook execution, no restored project output, no CI. Record opening,
PID and final actual exit. Preserve/archive/hash both success and first error.
If normal download fails, the known lossless recovery route is rendered
base64 -> one-shot loopback form -> exact expected SHA BEFORE local write.
Do not spend another series of file-panel/PNG/virtual-clipboard retries.
The verifier must pass against actual archive bytes before any success claim.
After evidence is local, disconnect/delete the runtime; preserve only a
bounded explicitly prepared HOT follow-up when one exists.

Scope: owner_B(image_(B*m)(n))=image_m(owner_B(n)), fixed-image injectivity,
owner-equality iff, unchanged scalar indicator action. Not injectivity across
different image indices, a permutation of the original half-open rectangle,
physical Q identification, a Neumann right inverse or a decay estimate.
Counters20/41,TermSource0,window15 unattained remain unchanged.

## Live checkpoint07:35UTC

Notebook vehiclef1df4d6fc87cc05bd7cabef99df79548f785992d,
launcher checkpointc11312dfec5a9a3452a058a08fad3933dc919333,
launcher SHA2568a5d7a91798be790f4b70cf0a7d114787609799a455b08154647f5747cac5568.
Notebook/launcher AST and one-execution static gate passed exit0/0.0989934s,
11620352observed peak bytes. Source and RUNNER_REV each assigned once.

Opened one Colab runtime, accountlluiseriksson@gmail.com, CPU/highRAM
50.99028778076172GiB/noGPU, hoste79819ba96b5. GitHub-source warning was
confirmed once before execution. Actual HASH_GATE=PASS and launch time
2026-09-06T07:33:47.757424+00:00, PID1677, original cell execution[1].
The same PID was confirmed live at47seconds; toolchain/checkout/source blobs
and three-file text/import guards had passed, stage lake_update in progress.
No mathematical verdict yet. Browser3 is the only tab; do not rerun this cell.

Console /content/neumann-integer-image-counting-diagnostic-v1-console.log.
Detailed current log /content/neumann-integer-image-counting-diagnostic-v1-launch/image_diagnostic.log.
Expected outer /content/neumann-integer-image-counting-diagnostic-v1-preservation-20260906.tar.gz.
Expected inner /content/hrpoly-neumann-integer-image-counting-diagnostic-v1-evidence.tar.gz.
Inspect that same process/log, preserve the first error or completed evidence,
then independently verify with the published reader. No PRE-VALIDATION change
is authorized by a running prefix or by the Mathlib-only stage alone.
