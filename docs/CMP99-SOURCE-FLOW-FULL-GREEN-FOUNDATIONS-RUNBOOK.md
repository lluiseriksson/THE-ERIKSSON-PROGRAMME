# Source-flow full-G foundations: bounded Colab queue

Source checkpoint A: `098cf3dfad1095c57bdc1d1dc5bc6b13252174d5`.
Parent cold seal: `bfa7b030618e26ee93312460778c9ca23fefbe83` (steps 14--15).
All four new Lean source/audit headers remain PRE-VALIDATION and are absent
from the root aggregator. Neither the source-flow inverse identity nor the
scalar foundation package is compiler evidence yet.

Use one new Colab Pro+ CPU/high-RAM runtime, account lluiseriksson@gmail.com.
Do not execute the obsolete cell in the notebook used to host the terminal.
Load the runner/verifier by raw committed URL and SHA256, with hashes obtained
from Git blobs, not the Windows worktree. Before the runner, run the verifier's
synthetic self-test with its two hash-pinned helpers. The runner separately
executes the exact axiom-gate self-test and portable 0/7 exit/timer preflight.

One fresh checkout, no restored project build, pinned Lean 4.29.0-rc6 and
Mathlib 07642720480157414db592fa85b626dafb71355b. Stop on first real child error:

1. Source scalar foundations focal, then exact four-name audit.
2. Source-flow full point-source inverse identity focal, then exact one-name audit.

The two targets share this checkout and its incremental build. Do not repeat
a cell or launcher whose process is still active. On failure preserve the
first raw error and complete evidence before a bounded hot repair. On complete
PASS use the read-only verifier, download/archive/log/exit/hash preservation,
then disconnect/delete the runtime. A prefix PASS does not seal the whole queue.

The moment/scalar-budget equality is not an identity between G and G Q'^*.
The Green identification uses the source-flow inverse of the literal full-box
precision with independent RG ratio L and localization K. No generated/source
coefficient equality, arbitrary point-source identity, uniform full amplitude,
regional B0, derivative estimate or window-15 attainment is supplied as a premise.
Counters remain 20/41, TermSource=0 even if this intermediate queue passes.
