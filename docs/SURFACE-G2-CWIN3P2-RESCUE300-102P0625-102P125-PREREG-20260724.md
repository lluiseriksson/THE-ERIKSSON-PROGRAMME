# CWIN=3/2 rescue preregistration: `[1633/16,817/8]`

**Registered:** 2026-07-24, before reading the run output

This is the next exact adjacent frontier unit after the completed
`[102,1633/16]` candidate.  The fixed beta interval is

```text
[1633/16,817/8] = [102.0625,102.125].
```

The contract is unchanged: `CWIN=3/2`, beta Taylor order 40, `t` order 50,
Arb precision 300 bits, and `min_dt=1/100000`.  The driver must cover
`[3/5, pi-(3/2)/hi]` with adjacent rational `t` boxes and strict negative
outward-rounded upper bounds.  Production and independent replay must be
byte-identical and pass the rescue validator.  This remains candidate-only
sign evidence; it cannot promote G2/G6, discharge `(H_tail)`, or prove the
sign-to-tail relay.  Endpoints and constants are frozen before execution.
