# Scaled bulk `[31,35]`: production preregistration

**Status:** `PREREGISTERED_CANDIDATE`

Frozen units are `[31,32]`, `[32,33]`, `[33,34]`, and `[34,35]`, each split
into ten beta boxes of width `1/10`. The contract is `J_m=e^{-beta}I_m`,
`CWIN=4`, beta Taylor order 12, t Taylor order 9, Arb precision 180 bits, and
the existing moving boundary `t<=pi-3/(2 beta)`. Every production transcript
must print provenance and all beta rows; a fresh replay must reproduce the rows
exactly. The independent validator requires a unique adjacent union from
31.0 to 35.0 and positive terminal rows. These units remain candidate
evidence and carry no G2/G6 load until the complete union and tail audit are
promoted.
