# K4 t-box [2.96,2.965] boundary failure — 2026-07-26

The generic endpoint t-box driver was run with the frozen contract on
`t=[2.96,2.965]` and the two delta boxes `[0.048,0.049]`, `[0.049,0.05]`.
Production and replay are byte-identical, but the validator rejects the box:

```text
nuD_main = [1.00965774046389 +/- 3.21e-15]
```

The other six aggregate rows are below one.  The failed transcripts are
retained in `scripts/surface_remainder_k4_tbox_2960_2965.txt` and its replay;
no positive manifest exists.  The preceding width-`0.005` boxes pass, so the
next admissible repair is an explicit width-`0.0025` split, not interpolation.
