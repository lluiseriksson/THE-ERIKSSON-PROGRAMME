# Draft only — C6d localized retained dependency-boundary cold seal

Do not copy this text into tracked documentation before the active cold run
prints literal `FINAL_STATUS=PASS`.

## Proposed verification-ledger addendum

## Addendum 944 (2026-08-25, **C6d localized retained source tower sealed in fresh Colab Pro+; 20/41 unchanged**)

PRE-VALIDATION checkpoint `b70735b82216a0ab1cd9a3bd4e195db1426a83fe`
fixed the source-facing localized/canonical retained-prefix producer and its
three-declaration audit.  That immutable graph contains seventeen visibly
PRE-VALIDATION modules from the retained exact-read/locality substrate through
the three Eq. (3.35) physical consumers; every corresponding audit is imported
by `YangMillsCore`.  The exact top source, top audit and `YangMillsCore.lean`
Git-blob SHA-256 values are respectively
`a0126fc772b50d066752f95b356dd963b9edb412dfc6caac4eef55f6b72e752a`,
`6333e4f75eca47726a94ff441658ba777dc064cdeee22ad5a37ce24293648d41`
and
`dabba854357f0abf6b2d994e5a96efc8271358d6c45c66dea9a087cfa4bb2479`.

Runner checkpoint `737b7b01badeba5811b1bb7ae557c6ea45c4a79e`,
notebook checkpoint `d07aa0267c0e5b3a43d8f091a77759da39161fd3`,
revision `c6d-localized-retained-tower-cold-v6`, and runner Git-blob
SHA-256
`c854661508990dc2f0335699ba6a066f11c22a9be72749bd2fbe825c39a7ac3c`
pin that source.  The first fresh CPU/high-RAM attempt lost its observable
runtime before a terminal sentinel and is classified
`INCOMPLETE-RUNTIME-LOSS`; it supplies neither PASS nor FAIL evidence.

The next cold attempt used source checkpoint
`16aee829d499c8d83b8ab54b46506b593b7e79e8`.  Its focal and direct audit
completed, but the cold root stopped at the first transitive failure in
`BalabanCMP99Eq335PhysicalRetainedNearIdentity` after `6170.283` seconds,
exit `1`; no seal was made.  Diagnostic evidence SHA-256 is
`123DACE6E5414B156284637E07677F6FA8C962E55F3F455C5081CFDB403750D8`
and archive SHA-256 is
`5720EF43C88E2ADDCCB6ED24389719C3B5C6BD157863E96E24675E2E51C1B2A51`.
Checkpoint `c3ad434890d8802a3d0b6ec5697862d488bba60f` fixes exactly that
specialization boundary; the controlled repetition below validates the
repaired immutable graph.

Before sealing, the declaration-coverage gate found eleven public C6d
declarations without an audit readout.  Checkpoint
`b91d42630efb15f32d4b4ecd242a33238926d4de` adds exactly those readouts;
the fail-closed coverage check now reports `92/92`.  Any green result at the
earlier `c3ad4348...` graph is retained as compiler evidence for that graph
but is not sufficient to claim total declaration coverage or retire marks.

Before the corrected gate was launched, static review found the Unicode typo
`≃₁` in the separately promoted Eq. (3.37) complex-coordinate module.
Checkpoint `b70735b8...` changes it to the actual `LinearEquiv` notation
`≃ₗ`; the v5 runner/notebook are superseded and were not reused.  This change
does not alter any of the 34 C6d boundary blobs, but the cold root and all
subsequent evidence are pinned to the corrected repository graph.

One controlled repetition checked out the corrected immutable source in a fresh
Colab Pro+ CPU/high-RAM runtime with Lean `4.29.0-rc6`, toolchain asset
SHA-256
`bf3e0a4025e47a0bea9ed907d12dcccd3d3590b1d8ad6c55a915298b01ad9d3e`
and Mathlib `07642720480157414db592fa85b626dafb71355b`.  The focal completed
[FOCAL_JOBS] jobs in `[FOCAL_SECONDS]` seconds, exit `[FOCAL_EXIT]`; the
explicit audit completed in `[AUDIT_SECONDS]` seconds, exit `[AUDIT_EXIT]`.
Its three declarations printed exactly
`[propext, Classical.choice, Quot.sound]` (the third list was line-wrapped by
Lean).  The cold `YangMillsCore` root completed [ROOT_JOBS] jobs in
`[ROOT_SECONDS]` seconds, exit `[ROOT_EXIT]`; the runner emitted literal
`FINAL_STATUS=PASS`.  Canonical evidence SHA-256 is `[EVIDENCE_SHA256]` and
archive SHA-256 is `[ARCHIVE_SHA256]`.  [ARCHIVE_RETENTION_SENTENCE]

The executed-notebook root transcript was then checked fail-closed against
the exact `#print axioms` declarations read from the seventeen audit blobs at
source checkpoint `b70735b8...`.  The verifier required all 92 distinct
declarations, rejected `sorryAx`, `ofReduceBool` and every axiom outside
`{propext, Classical.choice, Quot.sound}`, and emitted
`C6D_ROOT_TRANSITIVE_AXIOMS_OK`.  The verifier JSON SHA-256 is
`[TRANSITIVE_VERIFIER_JSON_SHA256]`; its extracted root-stage SHA-256 is
`[TRANSITIVE_ROOT_STAGE_SHA256]`.

Seal checkpoint `[SEAL_SHA]` retires exactly the 34 PRE-VALIDATION notices in
those seventeen source/audit pairs.  The top producer
constructs every retained localized/canonical prefix internally from the
source region chain, Ubar radius chain and sealed near-identity input, and
proves the terminal `Qprime` identity by projection from that constructed
package.  It does not accept a free tower or operator equality.

C6d supplies the finite f5 prefix and the three physical Eq. (3.35) consumer
interfaces only.  The four
fixed-depth CMP96 Proposition 2.2 actions and their per-depth Eq. (3.42)
assembler are already sealed; the exact next C6c.7 boundary is their
transport to the selected retained tower/source-region chain and the later
uniform-in-depth `B0, delta0` production.  No such transported or uniform
Green/action estimate is claimed here.  Window 15, rows 23--24 and
`TermSource` remain open.  Counters remain exactly `20/41`,
`TermSource = 0`; window 15 remains compatible but unattained.

## Proposed vertical-map step

### Step 8b.24/C6d: localized retained Eq. (3.35) dependency boundary ([PASS-STATE]; 20/41 unchanged)

`BalabanCMP99SourceLocalizedRetainedTower` uses one private recursion to
construct every localized and canonical retained prefix from the literal
source region chain.  It proves each prefix agrees definitionally with the
canonical retained identity extension and exports the terminal `Qprime`
`HEq`; no tower, small-field family or operator equality is accepted as a
free input.

[COPY THE EXACT COLD-EVIDENCE PARAGRAPH FROM ADDENDUM 944.]

The cold root plus the immutable-audit post-verifier certify all 92 axiom
readouts in the seventeen-module PRE-VALIDATION dependency boundary, including
the three immediate Eq. (3.35) physical consumers.  This step is still the
finite f5 source prefix and its consumer interfaces.  The four fixed-depth CMP96
Proposition 2.2 Green/action bounds and their per-depth Eq. (3.42) assembler
are already sealed elsewhere.  The exact next C6c.7 boundary is the
gauge/operator transport of those actions to the selected retained
tower/source-region chain, followed by uniform-in-depth `B0, delta0`
production.  Window 15, rows 23--24 and `TermSource` remain open; counters stay exactly `20/41`,
`TermSource = 0`, and window 15 remains compatible but unattained.

## Next cold queue after this seal

Promote and validate the six-file, eleven-declaration localized physical
precision package: internal-bond Laplacian carrier, source-region Laplacian
bridge, and the literal regional precision
`Delta'_a = Delta_U + a_j Q'^* Q'`, each with its audit.  That queue still
does not prove coercivity, a Green inverse, the later bridge from the selected
retained tower to the sealed fixed-depth CMP96 four-action Eq. (3.42)
certificate, or the uniform-in-depth `B0, delta0` obligation.
