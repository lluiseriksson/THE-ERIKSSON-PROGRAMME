# AXIOM_FRONTIER.md — v0.8.78 (RGCAUCHY P81 LIVE TARGET)

## Status
TOPOLOGICAL SU FRONT CLOSED LOCALLY; THE HAAR-LSI LANE NOW HAS A SINGLE SHORT PUBLIC FRONTIER ENTRYPOINT, EXPLICIT DIRECT/CONCRETE INPUT PACKETS, A CENTRALIZED INPUT-COHERENCE HUB, A UNIFIED END-TO-END EXPORT PACKET, A CENTRALIZED OBSTRUCTION PACKET, A UNIFIED PILLAR-II PACKET, AN EXPLICIT LOAD-BEARING PACKET, A DEDICATED LOAD-BEARING COHERENCE HUB, A UNIFIED AUDIT PACKET, A DEDICATED AUDIT COHERENCE HUB, AN EXPLICIT RGCAUCHY P81 KERNEL INTERFACE, AN EXPLICIT P81 OBLIGATION SURFACE, AN EXPLICIT P81 LIVE FRONTIER, AN EXPLICIT P81 COHERENCE PACKET, AND A SINGLE EXPLICIT P81 LIVE TARGET.

## Public lane
- `BalabanRGUniformLSIFrontier` remains the preferred short public entrypoint for the Haar-LSI lane.
- `RGCauchyP81KernelInput` and `RGCauchyP81KernelOutput` remain the preferred interface around the first unresolved RG–Cauchy theorem.
- `RGCauchyP81Obligation` remains the preferred single-name surface for the exact P81 decay obligation.
- `RGCauchyP81Frontier` remains the preferred short public frontier for the live P81 bottleneck.
- `RGCauchyP81CoherencePacket` remains the preferred coherence hub for the current P81 bottleneck names.
- `RGCauchyP81LiveTarget` now isolates, under one public name, the actual theorem-side weak-coupling target behind `rg_increment_decay_P81`.
- The theorem family `*_of_live_target` lets future work feed the theorem-side target directly into the kernel, obligation, frontier, and coherence layers.

## Remaining live mathematical obstruction
The P81 single-scale increment decay theorem and the actual Bałaban-RG package carrying uniform-LSI content still have to be discharged internally.

## Soundness note
This remains an honest reduction. No unconditional Haar-LSI theorem is claimed without the actual package-level witness and without the explicit P81 theorem-side live target.
