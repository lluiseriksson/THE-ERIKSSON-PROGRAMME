# AXIOM_FRONTIER.md — v0.9.36 (HAAR-LSI CONDITIONAL/LIVE TARGETS ALIGNED WITH THE DIRECT THEOREM TARGET)

## Status
TOPOLOGICAL SU FRONT CLOSED LOCALLY. THE P80/P81 CORRIDOR IS GREEN UNDER THE CURRENT ZERO-MAP SEMANTICS. THE P91 EXTERNAL RESIDUE AUDIT IS AT ZERO. `HaarLSIDirectBridge.lean` AND `HaarLSIFrontier.lean` ARE ALREADY ON THE ABSTRACT STACK. THIS STEP ALIGNS `HaarLSIConditionalClosure.lean` AND `HaarLSILiveTarget.lean` WITH THAT SAME DIRECT THEOREM TARGET.

## Public lane
- `HaarLSIDirectBridge.lean` remains the direct package → abstract target → Haar-LSI route.
- `HaarLSIFrontier.lean` remains the pkg-level frontier on the abstract route.
- `HaarLSIConditionalClosure.lean` now identifies its live conditional target with `SpecialUnitaryDirectUniformLSITheoremTarget`.
- `HaarLSILiveTarget.lean` now uses the same direct theorem target as its canonical public live target.

## Remaining live mathematical obstruction
The live obstruction is still:
- supplying the actual honest transfer `HaarLSIFromUniformLSITransfer`,
- reducing any remaining abstraction wrappers that no longer add mathematical content,
- replacing the placeholder zero-map RG semantics by the explicit Balaban blocking map,
- and rebuilding the theorem-side corridor there.

## Soundness note
No unconditional final Bałaban theorem is claimed here. This step only simplifies and aligns the live Haar-LSI target chain on the same abstract stack.
