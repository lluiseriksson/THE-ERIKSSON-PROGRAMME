# 🛑 dashboard/ — [ARCHIVED] 🛑

> **This folder is preserved for git-history reference only.**
> The single JSON file inside is **not maintained**.
> The replacement dashboard spec is `OBSERVABILITY.md` in the
> repo root.

This folder contains a single JSON file `current_focus.json` that
was last updated **2026-03-12** (45 days ago at the time of
updating this README, 2026-04-26).

Its content describes "Phase 0 — Sanitation and architecture" with
primary target `L0.build_validation`, which is **no longer the
active state of the project**. The current state is:

- **Version**: v2.47.0 (Codex sync of 2026-04-26 with anchored
  root-shell code, tail splitter, and reachability API)
- **Phase**: F3 frontier closure (BFS/Klarner decoder —
  recursive deletion/parent map is the next hard step)
- **Active priority**: Klarner BFS-tree count witness in
  `YangMills/ClayCore/LatticeAnimalCount.lean` (per
  `CODEX_CONSTRAINT_CONTRACT.md` §4 Priority 1.2)
- **Critical-path progress**: anchored root-shell layer landed
  in v2.42–v2.47; recursive parent/deletion layer pending

For the current state of the project, see (in order):

1. `FINAL_HANDOFF.md` (repo root) — 60-second TL;DR
2. `STATE_OF_THE_PROJECT.md` — current snapshot
3. `README.md` — bars, current front, "Last closed" version
4. `OBSERVABILITY.md` — dashboard spec (replacement for this folder)

The `current_focus.json` file in this folder is preserved for
historical reference but is **not maintained**. See
`COWORK_FINDINGS.md` Finding 007 for the dual-governance
dead-weight discussion and proposed cleanup options.

---

*README added 2026-04-25 by Cowork agent as defensive staleness
flag pending Lluis decision per Finding 007.*

*Banner upgraded to `[ARCHIVED]` 2026-04-26 by Cowork agent
(per Lluis go-ahead on Finding 007 Option A). JSON preserved
in place for git-history reference; not maintained.*
